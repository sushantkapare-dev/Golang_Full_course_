package main

import (
	"fmt"
	"strings"
)

func main() {

	FirstName := "sushant"
	LastrName := "kapare"
	FullName := FirstName + " " + LastrName

	fmt.Println(FullName)

	fmt.Println(strings.ToUpper(FullName))

}
